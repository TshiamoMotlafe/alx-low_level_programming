#include "main.h"
#include <stdio.h>

/**
 * main - Entry point of the program.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int i = 0;
    
    positive_or_negative(i);
    return (0);
}
